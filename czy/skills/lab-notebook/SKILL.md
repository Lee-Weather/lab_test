---
name: "lab-notebook"
description: "管理 Exp1 X1 训练实验笔记。目标文件 czy/exp1/实验记录/exp1.md。当用户要记录实验结果、新建实验、更新索引表、写回训练分析结论时使用。与 isaac-diag-eval 配合：本 skill 管笔记结构与编号，isaac-diag-eval 管数据分析（可选，有 CSV 时使用）。"
---

# Exp1 实验笔记更新规范

## 目标文件

```
czy/exp1/实验记录/exp1.md
```

**分工**：

| skill | 职责 |
| --- | --- |
| **lab-notebook（本 skill）** | 编号、索引表、实验章节结构、写回结论 |
| `isaac-diag-eval`（可选） | 跑 `eval_isaac_diag.py`，产出 E1~E8 诊断数值（需 CSV，无 CSV 时用训练日志数据） |

新建/更新实验必须遵循本 skill 的章节模板。

---

## 一、实验编号规则

### 1.1 格式（阶段 + 阶段内尝试）

```
exp{阶段}.{尝试}    或    exp{阶段}（阶段首次尝试可省略 .1）
```

- **阶段（主编号 `exp{n}`）**：对应一个**阶段目标**（如 reward≥25, ep_len≥900）。阶段目标未达成前，所有尝试都归入同一阶段。
- **尝试（子编号 `.{m}`）**：阶段内的第 m 次尝试。`exp1` 不达标 → 微调优化 → `exp1.1`；仍不达标 → `exp1.2`…
- **阶段晋级**：只有某次尝试**达标**后，才进入下一阶段 `exp{n+1}`。

| 示例 | 含义 | 何时使用 |
| --- | --- | --- |
| `exp0` | 基线（首个实验） | 独立保留，不归入任何阶段 |
| `exp1` | 阶段1 首次尝试 | 设定阶段目标后开训 |
| `exp1.1` | 阶段1 第2次尝试（微调1） | exp1 未达标，微调后重训 |
| `exp1.2` | 阶段1 第3次尝试（微调2） | exp1.1 仍未达标，继续微调 |
| `exp2` | 阶段2 首次尝试 | exp1.x 已达标，进入新阶段（新目标） |

**规则**：
- 编号不可变：失败尝试保留编号，索引表状态标「失败」，不删除不跳号
- `exp0` 是特殊基线，不参与阶段编号

### 1.2 微调（新建子编号 `.{m}`，不晋级阶段）

`exp{n}` **未达标**时，修改参数后重训 → 新建 `exp{n}.{m}`（m 递增）：

- 微调范围：仅改 1~3 项奖励权重、小参数、小幅修正
- 微调继承父实验的修改编号（见 §3）

### 1.3 晋级（新建主编号 `exp{n+1}`）

满足**任意一条**时，阶段晋级：

- `exp{n}` 或其微调 `exp{n}.{m}` **达标阶段目标** → 新阶段 `exp{n+1}` 设新目标
- 跨 ≥2 个模块的大改（新增/删除奖励、改 IK 逻辑、改 URDF）→ 即使未达标也晋级
- 切换训练任务（如 Flat → Rough）→ 晋级并在 §5 标注 task 变化

### 1.4 原地更新（不改编号）

满足**任意一条**时，不新建编号，直接更新现有实验的 §7：

- 训练中途观察日志，仅做趋势分析（未训完）
- 训练完成后补充结果数据
- 发现记录有误，修正数值

---

## 二、索引表更新规则

文件顶部 `## 实验索引` 表格，**每新建实验必须先加一行**：

| 列 | 必填 | 说明 |
| --- | --- | --- |
| 编号 | ✅ | 如 `exp2` |
| 日期 | ✅ | `YYYY-MM-DD` |
| 摘要 | ✅ | 一句话：改了什么、结果判定 |
| 状态 | ✅ | 见下表 |
| Task ID | ✅ | GM 任务 ID，如 `TASK_20260730_200` |
| GM账号 | ✅ | 训练使用的 GM 账号邮箱，如 `yijed24226@barumart.com` |
| checkpoint | ✅ | 最终模型文件名，如 `model_3000.pt` |

**状态枚举**：

| 状态 | 何时使用 |
| --- | --- |
| 待训练 | 方案已写，未开训 |
| 训练中 / 续训中 | 正在跑 |
| 已测试 | 训练完成，结论已写 |
| 失败 | 未达验收目标，保留教训 |
| 已废弃 | 方向否定，不再续做 |

---

## 三、修改内容编号规则

§3「修改内容」内部用**修改一、修改二...** 编号：

- **微调（`exp{n}.{m}`）**：修改编号**继承**父实验。如 exp1 有「修改一、二」，exp1.1 接「修改三」。
- **晋级（`exp{n+1}`）**：修改编号**重置**，从「修改一」重新开始。

```markdown
## 实验 exp1（阶段1 首次尝试）
### 修改一：…
### 修改二：…

## 实验 exp1.1（阶段1 微调1，继承）
### 修改三：…    ← 接续 exp1

## 实验 exp2（阶段2，重置）
### 修改一：…    ← 重新从「修改一」开始
```

---

## 四、单条实验章节模板

章节标题：`## 实验 {编号}：{简短标题}`（编号为 `exp0` / `exp{n}` / `exp{n}.{m}`）

**顺序固定，不可调换**：

```markdown
## 实验 {编号}：{标题}

### 1. 上一实验结果与教训

> 数据：上一实验的训练日志 / isaac_diag CSV（如有）
> - 关键数值（从上一轮 §7 复制 2~4 项，必须量化）
> - 现象描述
>
> **核心教训**：
> - 证明了什么 / 否定了什么
> - 本轮要解决的具体问题

### 2. 本轮修改目标

- 目标1：…
- 目标2：…
- 验收标准：量化目标（如 Mean reward ≥ 25，Episode length ≥ 900）

### 3. 修改内容

### 修改一：{标题}

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| … | … | … | … |

**理由**：…

### 修改二：…

### 4. 修改文件

- `path/to/file.py`：改了什么

### 5. 训练参数

| 参数 | 值 |
| --- | --- |
| 训练方式 | 从零 / **续训** |
| GM账号 | 如 `yijed24226@barumart.com` |
| max_iterations | … |
| save_interval | … |
| num_envs | … |
| seed | … |
| learning_rate | … |
| 算力 | GPU型号，ESKU… |
| 镜像 | BJX…, V… |
| 代码仓库 | repo.git, 分支 |
| 启动命令 | `gm-run ...` |

### 6. 预期与验收

**目标指标**（训练日志，3000轮时）：

| 指标 | 上一轮 | 本轮目标 | 异常信号 |
| --- | --- | --- | --- |
| Mean reward | … | ≥ 25 | < 15 |
| Mean episode length | … | ≥ 900 | < 700 |
| track_lin_vel_xy_exp | … | … | … |
| track_ang_vel_z_exp | … | … | … |
| termination_penalty | … | … | … |

> 如有 isaac-diag-eval CSV，可补充 E1~E8 诊断指标。

### 7. 实验结果

> 训练任务：TASK_…，时间…
> 最终 checkpoint：model_….pt

#### 最终结果（iter …）

| 指标 | 上一轮 | 目标 | 实测 | 判定 |
| --- | --- | --- | --- | --- |
| Mean reward | … | ≥ 25 | … | ✅/❌ |
| Mean episode length | … | ≥ 900 | … | ✅/❌ |

#### 训练趋势

| iter | Mean reward | Mean episode length |
| --- | --- | --- |
| … | … | … |

#### 各奖励项最终值

| 奖励项 | 权重 | 最终值 | 说明 |
| --- | --- | --- | --- |
| … | … | … | … |

**结论**：（✅达标 / ⚠️部分达标 / ❌未达标 + 一句话）

**根因分析**（失败或部分达标时必填）：

- …

**下一轮方向**：

- …
```

### 4.1 首个实验（exp0）

`### 1. 上一实验结果与教训` 写：

```markdown
> 本轮为 exp 系列首个实验，无上一轮数据。
> 基于上游仓库默认配置直接开训，作为基线。
```

### 4.2 训练前 vs 训练后

| 时机 | 应更新的节 |
| --- | --- |
| 开训前 | 索引表 + §1~§6 写全；§7 留「待训练完成后补充。」 |
| 训练中 | 可在 §7 追加中间趋势分析（不改编号） |
| 训完后 | 用 GM 数据接口拉训练曲线 → 填 §7 → 更新索引表状态 |

---

## 五、Agent 工作流

### 5.1 新建实验

1. **判断编号**：
   - 上一实验**达标**（或跨模块大改/换任务）→ 晋级 `exp{n+1}`（修改编号重置）
   - 上一实验**未达标**且仅小改 → 微调 `exp{n}.{m}`（修改编号继承父实验）
2. 在索引表**末尾**追加一行
3. 在文件**末尾**追加完整实验章节（§1~§7，§7 占位「待训练完成后补充。」）
4. §1 必须引用上一实验 §7 的**具体数值**，禁止只写「效果不好」

### 5.2 训练完成后更新结果

1. 用 `gm task data get` 拉训练曲线关键点数据
2. 将 Mean reward、Episode length、各奖励项最终值填入 **§7**
3. 写分级结论（✅达标 / ⚠️部分达标 / ❌未达标）
4. 更新索引表：状态 → `已测试` 或 `失败`；摘要补关键判定

### 5.3 训练中途更新（不改编号）

1. 不新建实验，直接在现有 §7 追加「训练进度趋势」小节
2. 索引表状态保持「训练中」

### 5.4 禁止事项

- ❌ 不编造未拉取的数据
- ❌ 不删除失败实验记录
- ❌ 不跳号（exp2 之后是 exp3，不要直接 exp5）
- ❌ 结论禁止笼统「还行」，必须分级 + 量化

---

## 六、快速决策表

| 场景 | 编号 | 动作 |
| --- | --- | --- |
| 基线 | `exp0` | §1 写「无上一轮」 |
| 阶段1 首次尝试 | `exp1` | 设阶段目标开训 |
| 未达标，小改重训 | `exp1.1` | 微调，§3 修改编号继承 exp1 |
| 达标后开新目标 | `exp2` | 晋级，§3 修改编号重置 |
| 训练失败但教训明确 | 原编号 | 更新 §7 + 状态「失败」 |
| 训练中途看日志 | 原编号 | §7 追加趋势，不改编号 |
| 跨模块大改（未达标也晋级） | `exp{n+1}` | 重置修改编号 |
| 切换任务（Flat→Rough） | `exp{n+1}` | 晋级，§5 标注 task 变化 |

---

## 七、注意事项

1. **§1 教训必须可量化**：至少 2 个来自训练日志或 isaac_diag 的数
2. **失败同样完整记录 §7**：失败原因写在根因分析
3. **索引表与正文状态一致**
4. **结论分级**：✅达标 / ⚠️部分达标 / ❌未达标，禁止笼统表述
5. **GM 数据获取**：用 `gm task data get --task-id ... --data-key "Train/mean_reward"` 拉曲线
